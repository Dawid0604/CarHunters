spring:
  application:
    name: car-history-service

  output:
    ansi:
      enabled: ALWAYS

  config:
    import: optional:configserver:http://${CONFIG_SERVICE_INSTANCE:localhost}:8888

mongock:
  migration-scan-package: pl.dawid0604.carhunters.car.history.service.config.migration